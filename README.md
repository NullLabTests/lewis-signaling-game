# Lewis Signaling Game

Minimal implementation of a Lewis signaling game (sender-receiver) in Python.
- Sender observes a state and sends one of K signals.
- Receiver receives signal and chooses an action.
- Reward granted if receiver's action matches the true state.
This repo contains a simple tabular learning baseline (reinforcement-like updates)
and scripts to run experiments and plot results.
