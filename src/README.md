# Lewis Signaling Game

Minimal implementation of a Lewis signaling game (sender-receiver) in Python.

Note: run.py forces matplotlib to use the 'Agg' (non-GUI) backend so it works in headless/CI environments.

Run:
  python3 -m src.run
